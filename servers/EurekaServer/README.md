# Eureka Server for Workshop Labs

To startup a Eureka Server, run this project as a Spring Boot app by issuing:

```bash
./mvnw spring-boot:run
```

Eureka will start up on port 8761 and serve the Eureka API from "/eureka".